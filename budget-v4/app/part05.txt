).toLocaleString('ru-RU').replace(/\u00a0/g,' ')+' ₽'}
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]))}
function monthOf(d){return String(d).slice(0,7)}function dateObj(s){const [y,m,d]=s.split('-').map(Number);return new Date(y,m-1,d)}function isoDate(d){return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}function addDays(s,n){const d=dateObj(s);d.setDate(d.getDate()+n);return isoDate(d)}function ruDate(s,opts={weekday:'long',day:'numeric',month:'long'}){return dateObj(s).toLocaleDateString('ru-RU',opts)}
function amountFor(c,m,overrides={}){if(overrides[c.id]!=null)return Number(overrides[c.id]);if(c.overrides&&Object.prototype.hasOwnProperty.call(c.overrides,m))return Number(c.overrides[m])||0;if(c.from&&m<c.from)return 0;if(c.schedule==='once')return c.date&&monthOf(c.date)===m?Number(c.amount)||0:0;if(c.id==='journal'&&Array.isArray(c.months))return c.months.includes(m)?Number(c.amount)||0:0;return Number(c.amount)||0}
function category(id){return state.categories.find(c=>c.id===id)}function scheduleText(c){if(c.schedule==='fixed')return c.day+'-го';if(c.schedule==='range')return c.dayFrom+'–'+c.dayTo+'-го';if(c.schedule==='variable')return 'без фиксированной даты';if(c.schedule==='floating')return 'плавающая дата';if(c.schedule==='once')return ruDate(c.date,{day:'numeric',month:'short',year:'numeric'});return ''}
function occurrenceDate(c,m){if(c.from&&m<c.from)return null;const [y,mo]=m.split('-').map(Number),days=new Date(y,mo,0).getDate();if(c.schedule==='fixed')return `${m}-${String(Math.min(Number(c.day),days)).padStart(2,'0')}`;if(c.schedule==='range')return `${m}-${String(Math.min(Number(c.dayTo),days)).padStart(2,'0')}`;if(c.schedule==='once'&&c.date&&monthOf(c.date)===m)return c.date;return null}
function rangeActive(c,date){if(c.schedule!=='range')return false;const d=Number(date.slice(8,10));return d>=Number(c.dayFrom)&&d<=Number(c.dayTo)&&(!c.from||monthOf(date)>=c.from)}
function plannedDay(date){const m=monthOf(date),cats=[];state.categories.forEach(c=>{const od=occurrenceDate(c,m);if(od===date)cats.push({source:'cat',id:c.id,kind:c.kind,amount:amountFor(c,m),name:c.name,note:c.schedule==='range'?`Окно ${c.dayFrom}–${c.dayTo}-го · ${c.note||''}`:c.note||'',payer:'me'});else if(rangeActive(c,date))cats.push({source:'window',id:c.id,kind:'note',amount:0,name:c.name,note:`Окно оплаты ${c.dayFrom}–${c.dayTo}-го. В расчёте сумма учитывается один раз — ${c.dayTo}-го.`,payer:'me'})});const ev=state.plannedEvents.filter(e=>e.date===date).map(e=>({source:'event',...e}));return [...cats,...ev]}
function eventStream(from,to,opts={}){const overrides=opts.overrides||{},excludeIds=new Set(opts.excludeIds||[]),a=[];let d=from;while(d<=to){const m=monthOf(d);state.categories.forEach(c=>{if(excludeIds.has(c.id))return;const od=occurrenceDate(c,m);if(od===d){const amt=amountFor(c,m,overrides);if(amt)a.push({date:d,id:c.id,name:c.name,kind:c.kind,amount:amt,certainty:'fixed',source:'category'})}});state.plannedEvents.filter(e=>e.date===d&&e.kind!=='note'&&e.payer!=='other').forEach(e=>a.push({date:d,id:e.id,name:e.name,kind:e.kind,amount:Number(e.amount)||0,certainty:'fixed',source:'event'}));d=addDays(d,1)}return a}
function nextIncome(after){for(let i=1;i<=75;i++){const d=addDays(after,i),rows=eventStream(d,d).filter(x=>x.kind==='income');if(rows.length)return {date:d,amount:rows.reduce((s,x)=>s+x.amount,0),names:rows.map(x=>x.name)}}return null}
function nextOccurrence(catId,after){const c=category(catId);if(!c)return null;for(let i=0;i<370;i++){const d=addDays(after,i),od=occurrenceDate(c,monthOf(d));if(od===d&&d>after)return {date:d,amount:amountFor(c,monthOf(d)),cat:c}}return null}
function txMonth(m){return state.transactions.filter(t=>monthOf(t.date)===m)}function categorySpent(id,m){return txMonth(m).filter(t=>t.kind==='expense'&&t.categoryId===id&&!t.paidByOther).reduce((s,t)=>s+Number(t.amount||0),0)}
function planMonth(m){let inc=0,exp=0;state.categories.forEach(c=>{const a=amountFor(c,m);if(c.kind==='income')inc+=a;else exp+=a});const sav=Number(state.savings[m]||0);return{inc,exp,sav,free:inc-exp-sav}}
function monthList(){const a=[];for(let i=0;i<16;i++){const d=new Date(2026,8+i,1);a.push(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`)}return a}
function projectedBalance(to,opts={}){let bal=Number(state.currentBalance)||0;const from=state.balanceAsOf;const stream=eventStream(from,to,{overrides:opts.overrides||{},excludeIds:opts.excludeIds||[]});stream.forEach(e=>{bal+=e.kind==='income'?e.amount:-e.amount});return {balance:bal,events:stream}}
function cashflow(from,to,opts={}){const stream=eventStream(from,to,opts);let inc=0,exp=0;stream.forEach(e=>e.kind==='income'?inc+=e.amount:exp+=e.amount);return{inc,exp,net:inc-exp,events:stream}}
function upcoming(days=14){const a=[];for(let i=0;i<=days;i++){const d=addDays(state.selectedDate,i);plannedDay(d).forEach(x=>{if(x.source==='window')return;a.push({...x,date:d})})}return a}
function variableSnapshot(m){return ['products','lunch','gas','cigs'].map(id=>{const c=category(id);return c?{id,name:c.name,target:amountFor(c,m),spent:categorySpent(id,m),remaining:Math.max(0,amountFor(c,m)-categorySpent(id,m))}:null}).filter(Boolean)}
function floatingSnapshot(m){return state.categories.filter(c=>c.schedule==='floating'&&amountFor(c,m)>0).map(c=>({id:c.id,name:c.name,amount:amountFor(c,m),note:c.note}))}
function buildSnapshot(question=''){const m=monthOf(state.selectedDate),ni=nextIncome(state.selectedDate),rent=nextOccurrence('rent',state.selectedDate);return{asOf:state.balanceAsOf,selectedDate:state.selectedDate,currentBalance:state.currentBalance,nextIncome:ni,nextRent:rent?{date:rent.date,amount:rent.amount}:null,monthPlan:planMonth(m),variable:variableSnapshot(m),floating:floatingSnapshot(m),upcoming:upcoming(45).map(x=>({date:x.date,name:x.name,kind:x.kind,amount:x.amount,payer:x.payer||'me'})),recentTransactions:state.transactions.slice().sort((a,b)=>b.date.localeCompare(a.date)).slice(0,30),categories:state.categories.map(c=>({id:c.id,name:c.name,kind:c.kind,amount:amountFor(c,m),schedule:c.schedule,day:c.day,dayFrom:c.dayFrom,dayTo:c.dayTo,note:c.note})),question}}
function parseMoney(q){const ms=[...q.matchAll(/(\d[\d\s]{2,})(?:\s*(?:₽|руб(?:лей|ля|ль)?))?/gi)];if(!ms.length)return null;let raw=ms[ms.length-1][1].replace(/\s/g,'');return Number(raw)||null}
const MONTHS={января:1,февраля:2,марта:3,апреля:4,мая:5,июня:6,июля:7,августа:8,сентября:9,октября:10,ноября:11,декабря:12,январь:1,февраль:2,март:3,апрель:4,май:5,июнь:6,июль:7,август:8,сентябрь:9,октябрь:10,ноябрь:11,декабрь:12};
function parseDate(q){q=q.toLowerCase();const m=q.match(/(?:^|\s)(\d{1,2})\s+(января|февраля|марта|апреля|мая|июня|июля|августа|сентября|октября|ноября|декабря|январь|февраль|март|апрель|май|июнь|июль|август|сентябрь|октябрь|ноябрь|декабрь)(?:\s|$|[,.!?])/);if(m){let y=dateObj(state.selectedDate).getFullYear(),mo=MONTHS[m[2]];if(mo<dateObj(state.selectedDate).getMonth()+1-2)y++;return `${y}-${String(mo).padStart(2,'0')}-${String(Number(m[1])).padStart(2,'0')}`}const iso=q.match(/(20\d{2})-(\d{2})-(\d{2})/);return iso?iso[0]:null}
function aliases(q){q=q.toLowerCase();const map={rent:['квартир','аренд'],products:['продукт','магазин','еда домой'],lunch:['обед'],gas:['бенз','заправ'],ai:['нейросет','подписк'],utilities:['коммун'],advance15:['аванс'],salary30:['зарплат','основн'],megafonNet:['мегафон','интернет'],photo:['фотограф'],kontur:['контур']};for(const [id,arr] of Object.entries(map))if(arr.some(a=>q.includes(a)))return id;return null}
function biggestIncomeOn(date){return eventStream(date,date).filter(e=>e.kind==='income').sort((a,b)=>b.amount-a.amount)[0]||null}
function rentAnalysis(q){const date=parseDate(q)||state.selectedDate,amount=parseMoney(q),ov={};let assumption='';if(amount&&/если|будет|прид[её]т|аванс|зарплат/.test(q.toL