til=ni?cashflow(state.balanceAsOf,addDays(ni.date,-1)):null;const pre=ni?state.currentBalance+(until?.net||0):state.currentBalance;$('balanceValue').textContent=fmt(state.currentBalance);$('heroNote').innerHTML=`Остаток зафиксирован на <b>${ruDate(state.balanceAsOf,{day:'numeric',month:'long'})}</b>. ${ni?`Перед следующим доходом по фиксированному плану: <b>${fmt(pre)}</b>.`:''}`;$('homeDate').value=state.selectedDate;$('dayTitle').textContent=ruDate(state.selectedDate);const rent=nextOccurrence('rent',state.selectedDate);$('situation').innerHTML=`<div class="stat"><div><span>Следующий доход</span><b>${ni?fmt(ni.amount):'—'}</b></div><div class="stat-side"><b>${ni?ruDate(ni.date,{day:'numeric',month:'short'}):'—'}</b><small>${ni?ni.names.join(', '):''}</small></div></div><div class="stat"><div><span>Перед этим доходом</span><b class="${pre<0?'bad':'good'}">${fmt(pre)}</b></div><div class="stat-side"><small>по фиксированным событиям</small></div></div><div class="stat"><div><span>Следующая квартира</span><b>${rent?fmt(rent.amount):'—'}</b></div><div class="stat-side"><b>${rent?ruDate(rent.date,{day:'numeric',month:'short'}):'—'}</b><small>помощник умеет считать резерв</small></div></div>`;const rows=plannedDay(state.selectedDate);$('dayList').innerHTML=rows.length?rows.map(x=>{const cls=x.kind==='income'?'income':x.kind==='expense'?'expense':'note',sign=x.kind==='income'?'+':x.kind==='expense'?'−':'';return `<div class="event"><div class="event-icon ${cls}">${x.kind==='income'?'↓':x.kind==='expense'?'↑':'•'}</div><div class="event-main"><b>${esc(x.name)}</b><small>${esc(x.note||'')}${x.payer==='other'?' · оплачивает другой человек':''}</small></div><div class="amount ${cls}">${x.amount?sign+fmt(x.amount):'—'}</div></div>`}).join(''):'<div class="empty">На этот день ничего не запланировано.</div>'}
function renderHistory(){const m=monthOf(state.selectedDate),tx=txMonth(m).slice().sort((a,b)=>b.date.localeCompare(a.date)),inc=tx.filter(t=>t.kind==='income'&&!t.paidByOther).reduce((s,t)=>s+Number(t.amount||0),0),exp=tx.filter(t=>t.kind==='expense'&&!t.paidByOther).reduce((s,t)=>s+Number(t.amount||0),0),other=tx.filter(t=>t.paidByOther).reduce((s,t)=>s+Number(t.amount||0),0);$('historyMetrics').innerHTML=`<div class="kpi"><span>Доходы</span><b class="good">${fmt(inc)}</b></div><div class="kpi"><span>Расходы</span><b class="bad">${fmt(exp)}</b></div><div class="kpi"><span>Оплатили другие</span><b>${fmt(other)}</b></div><div class="kpi"><span>Операций</span><b>${tx.length}</b></div>`;$('historyTable').innerHTML=tx.length?`<table><thead><tr><th>Дата</th><th>Операция</th><th>Категория</th><th>Кто</th><th class="num">Сумма</th><th></th></tr></thead><tbody>${tx.map(t=>`<tr><td>${ruDate(t.date,{day:'numeric',month:'short'})}</td><td><b>${esc(t.name||'Без названия')}</b></td><td>${esc(category(t.categoryId)?.name||'—')}</td><td>${t.paidByOther?'Другой':'Я'}</td><td class="num ${t.kind==='income'?'good':'bad'}">${t.kind==='income'?'+':'−'}${fmt(t.amount)}</td><td><button class="micro" data-edit-tx="${t.id}">изменить</button></td></tr>`).join('')}</tbody></table>`:'<div class="empty">Пока нет операций.</div>'}
function renderPlan(){const sel=$('planMonth');if(!sel.options.length)sel.innerHTML=monthList().map(m=>`<option value="${m}">${ruDate(m+'-01',{month:'long',year:'numeric'})}</option>`).join('');if(!monthList().includes(state.planMonth))state.planMonth='2026-09';sel.value=state.planMonth;const p=planMonth(state.planMonth);$('planMetrics').innerHTML=`<div class="kpi"><span>Доход</span><b class="good">${fmt(p.inc)}</b></div><div class="kpi"><span>Расходы</span><b class="bad">${fmt(p.exp)}</b></div><div class="kpi"><span>В подушку</span><b class="warn">${fmt(p.sav)}</b></div><div class="kpi"><span>Свободно</span><b>${fmt(p.free)}</b></div>`;const row=c=>`<div class="list-row"><div><b>${esc(c.name)}</b><small>${scheduleText(c)}${c.note?' · '+esc(c.note):''}</small></div><div class="amount ${c.kind}">${fmt(amountFor(c,state.planMonth))}</div></div>`;$('planIncome').innerHTML=state.categories.filter(c=>c.kind==='income'&&amountFor(c,state.planMonth)).map(row).join('');$('planExpense').innerHTML=state.categories.filter(c=>c.kind==='expense'&&amountFor(c,state.planMonth)).map(row).join('')}
function renderCategories(){$('categoriesTable').innerHTML=`<table><thead><tr><th>Статья</th><th>Когда</th><th class="num">Ориентир</th><th></th></tr></thead><tbody>${state.categories.map(c=>`<tr><td><b>${esc(c.name)}</b><div style="font-size:10px;color:var(--muted);margin-top:3px">${c.kind==='income'?'доход':'расход'}</div></td><td><span class="badge ${['variable','floating'].includes(c.schedule)?'variable':'fixed'}">${esc(scheduleText(c))}</span></td><td class="num">${fmt(c.amount)}</td><td><button class="micro" data-cat="${c.id}">изменить</button></td></tr>`).join('')}</tbody></table>`}
function renderAssistant(){const ni=nextIncome(state.selectedDate),rent=nextOccurrence('rent',state.selectedDate),vars=variableSnapshot(monthOf(state.selectedDate));$('assistantContext').innerHTML=`<div class="context-row"><span>На руках</span><b>${fmt(state.currentBalance)}</b></div><div class="context-row"><span>Следующий доход</span><b>${ni?fmt(ni.amount)+' · '+ruDate(ni.date,{day:'numeric',month:'short'}):'—'}</b></div><div class="context-row"><span>Квартира</span><b>${rent?fmt(rent.amount)+' · '+ruDate(rent.date,{day:'numeric',month:'short'}):'—'}</b></div><div class="context-row"><span>Продукты</span><b>${fmt(vars.find(x=>x.id==='products')?.spent||0)} / ${fmt(vars.find(x=>x.id==='products')?.target||0)}</b></div><div class="context-row"><span>Обеды</span><b>${fmt(vars.find(x=>x.id==='lunch')?.spent||0)} / ${fmt(vars.find(x=>x.id==='lunch')?.target||0)}</b></div>`;const ep=(state.aiEndpoint||'').trim()|| (location.hostname.endsWith('vercel.app')?'/api/budget-assistant':'');$('aiStatus').classList.toggle('online',!!ep);$('aiStatus').querySelector('span').textContent=ep?'GPT‑5.6 + расчётный движок':'Умный локальный анализ'}
function renderMore(){if($('aiEndpoint'))$('aiEndpoint').value=state.aiEndpoint||'';renderCategories()}
function renderAll(){renderHome();renderHistory();renderPlan();renderAssistant();renderMore();save()}
function showScreen(name){document.querySelectorAll('.screen').forEach(x=>x.classList.toggle('active',x.id==='screen-'+name));document.querySelectorAll('[data-screen]').forEach(x=>x.classList.toggle('active',x.dataset.screen===name));renderAll()}
function openModal(id){$(id).classList.add('open')}function closeModal(id){$(id).classList.remove('open')}function fillTxCategories(kind){$('txCategory').innerHTML='<option value="">Без категории</option>'+state.categories.filter(c=>c.kind===kind).map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}
function openTx(kind='expense',tx=null){$('txTitle').textContent=tx?'Изменить операцию':kind==='income'?'Добавить доход':'Добавить расход';$('txId').value=tx?.id||'';$('txKind').value=tx?.kind||kind;fillTxCategories(tx?.kind||kind);$('txAmount').value=tx?.amount||'';$('txDate').value=tx?.date||state.selectedDate;$('txCategory').value=tx?.categoryId||'';$('txName').value=tx?.name||'';$('txPaidByOther').checked=!!tx?.paidByOther;$('txMakeRecurring').checked=false;$('deleteTx').classList.toggle('hidden',!tx);toggleRecurring();openModal('txModal')}
function toggleRecurring(){const on=$('txMakeRecurring').checked,s=$('recSchedule').value;['recNameField','recScheduleField'].forEach(id=>$(id).classList.toggle('hidden',!on));$('recDayField').classList.toggle('hidden',!on||s!=='fixed');$('recRangeField').classList.toggle('hidden',!on||s!=='range')}
function parseOverrides(s){const o={};String(s||'').split(';').map(x=>x.trim()).filter(Boolean).forEach(p=>{const [k,v]=p.split('=').map(x=>x.trim());if(k&&v!==undefined)o[k]=Number(v)||0});return o}function overridesText(o){return Object.entries(o||{}).map(([k,v])=>`${k}=${v}`).join('; ')}
function openCat(c=null){$('catTitle').textContent=c?'Изменить статью':'Н