owerCase())){let target=aliases(q);if(!target||category(target)?.kind!=='income'){const bi=biggestIncomeOn(date);target=bi?.id}if(target&&category(target)?.kind==='income'){ov[target]=amount;assumption=`Считаю, что ${fmt(amount)} — это «${category(target).name}» ${ruDate(date,{day:'numeric',month:'long'})}.`}}
 const rent=nextOccurrence('rent',date);if(!rent)return {answer:'Не нашёл следующую оплату квартиры в плане.',facts:[],actions:[]};const atDate=projectedBalance(date,{overrides:ov});const from=addDays(date,1),to=addDays(rent.date,-1),flow=from<=to?cashflow(from,to,{overrides:ov,excludeIds:['rent']}):{inc:0,exp:0,net:0,events:[]};const needed=Math.max(0,Math.min(rent.amount,rent.amount-flow.net));const possible=Math.max(0,atDate.balance);const short=Math.max(0,needed-possible);const salaryBefore=flow.events.find(e=>e.id==='salary30');let answer=`${assumption?assumption+'\n\n':''}На конец ${ruDate(date,{day:'numeric',month:'long'})} по текущему плану получится около ${fmt(atDate.balance)}. Квартира ${fmt(rent.amount)} — ${ruDate(rent.date,{day:'numeric',month:'long'})}.\n\nС ${ruDate(from,{day:'numeric',month:'short'})} до дня перед арендой гарантированные доходы: ${fmt(flow.inc)}, фиксированные платежи: ${fmt(flow.exp)}, чистый приток: ${fmt(flow.net)}. Поэтому минимально нужно «заморозить» на квартиру уже ${ruDate(date,{day:'numeric',month:'long'})}: ${fmt(needed)}.`;if(needed===0&&salaryBefore)answer+=`\n\nТо есть из денег ${ruDate(date,{day:'numeric',month:'long'})} на квартиру можно не отрезать сумму сразу, если ${salaryBefore.name.toLowerCase()} ${fmt(salaryBefore.amount)} придёт по плану ${ruDate(salaryBefore.date,{day:'numeric',month:'long'})}. В этот день я бы сразу отложил полные ${fmt(rent.amount)} и больше их не считал свободными.`;if(short>0)answer+=`\n\nНо на выбранную дату доступно только ${fmt(possible)}, поэтому до безопасного резерва не хватает ${fmt(short)}.`;answer+=`\n\nПлавающие траты — продукты, обеды, Мегафон, фотограф и бензин — в гарантированный расчёт не включены. Поэтому они уменьшают свободные деньги, но не меняют сумму аренды.`;const actions=[];if(amount&&ov.advance15!=null)actions.push({type:'override',categoryId:'advance15',month:monthOf(date),amount,label:`Учесть аванс ${fmt(amount)} в ${monthOf(date)}`});return{answer,facts:[`Прогноз на ${date}: ${fmt(atDate.balance)}`,`Аренда: ${fmt(rent.amount)} ${rent.date}`,`Гарантированный чистый приток до аренды: ${fmt(flow.net)}`,`Резерв на выбранную дату: ${fmt(needed)}`],actions}}
function untilDateAnalysis(q){const to=parseDate(q)||nextIncome(state.selectedDate)?.date;if(!to)return{answer:`Сейчас у тебя ${fmt(state.currentBalance)}. Не понял, до какой даты считать.`,facts:[],actions:[]};const low=q.toLowerCase(),inclusive=/\bк\s+\d{1,2}/.test(low);const end=inclusive?to:addDays(to,-1);const p=projectedBalance(end);const vars=variableSnapshot(monthOf(state.selectedDate));const remainingVars=vars.reduce((s,x)=>s+x.remaining,0);const label=inclusive?`к ${ruDate(to,{day:'numeric',month:'long'})}`:`до начала ${ruDate(to,{day:'numeric',month:'long'})}`;return{answer:`По фиксированным событиям ${label} прогноз — ${fmt(p.balance)}. Это расчёт от текущих ${fmt(state.currentBalance)} с учётом запланированных фиксированных приходов и оплат.\n\nПеременные траты не списываю автоматически: по продуктам, обедам, бензину и другим категориям их нужно заносить фактом. Если потратить весь оставшийся месячный ориентир по переменным категориям (${fmt(remainingVars)}), запас будет заметно меньше.`,facts:p.events.slice(0,10).map(e=>`${e.date}: ${e.kind==='income'?'+':'−'}${fmt(e.amount)} ${e.name}`),actions:[]}}
function spendScenario(q){const amount=parseMoney(q);if(!amount)return null;const ni=nextIncome(state.selectedDate);if(!ni)return null;const currentAfter=state.currentBalance-amount,flow=cashflow(state.selectedDate,addDays(ni.date,-1));const after=currentAfter+flow.net;return{answer:`Если потратить ${fmt(amount)} сейчас, на руках останется ${fmt(currentAfter)}. До следующего фиксированного дохода ${ruDate(ni.date,{day:'numeric',month:'long'})} по известным фиксированным событиям итог будет около ${fmt(after)}. ${after<0?'Это создаёт кассовый разрыв до дохода.':'Фиксированный план выдерживает такую трату.'}\n\nПеременные расходы сверх уже внесённых операций сюда не добавлены автоматически.`,facts:[`Сейчас после покупки: ${fmt(currentAfter)}`,`Фиксированный поток до дохода: ${fmt(flow.net)}`,`Перед доходом: ${fmt(after)}`],actions:[]}}
function categoryAnalysis(q){const id=aliases(q),m=monthOf(state.selectedDate);if(!id)return null;const c=category(id);if(!c)return null;if(c.kind==='expense'&&['variable','floating'].includes(c.schedule)){const spent=categorySpent(id,m),target=amountFor(c,m);return{answer:`По статье «${c.name}» в ${ruDate(m+'-01',{month:'long'})} внесено фактом ${fmt(spent)}. Ориентир месяца — ${fmt(target)}, остаётся до ориентира ${fmt(Math.max(0,target-spent))}. ${c.note||''}`,facts:[],actions:[]}}return null}
function proposedTransaction(q){const amount=parseMoney(q);if(!amount)return null;const low=q.toLowerCase(),date=parseDate(q)||state.selectedDate;let kind=null;if(/купил|потратил|заплатил|оплатил|ушло/.test(low))kind='expense';if(/получил|пришло|заработал|выплатили/.test(low))kind='income';if(!kind)return null;const id=aliases(q),c=id?category(id):null;if(c&&c.kind!==kind)return null;return{type:'transaction',kind,amount,date,categoryId:id||'',name:c?.name||(kind==='expense'?'Расход':'Доход'),label:`Учесть ${kind==='expense'?'расход':'доход'} ${fmt(amount)} · ${ruDate(date,{day:'numeric',month:'short'})}`}}
function smartLocal(q){const low=q.toLowerCase();let r;if(/квартир|аренд/.test(low)&&/отлож|резерв|сколько/.test(low))r=rentAnalysis(q);else if(/если.*потрат|могу.*потрат|купить.*\d/.test(low))r=spendScenario(q);else if(/хватит|до\s+\d|дожить/.test(low))r=untilDateAnalysis(q);else if(low.includes('продукт')&&low.includes('обед')){const m=monthOf(state.selectedDate),p=category('products'),l=category('lunch'),ps=categorySpent('products',m),ls=categorySpent('lunch',m);r={answer:`В ${ruDate(m+'-01',{month:'long'})} по продуктам внесено ${fmt(ps)} из ориентира ${fmt(amountFor(p,m))}; по обедам — ${fmt(ls)} из ${fmt(amountFor(l,m))}. Остаток ориентиров: продукты ${fmt(Math.max(0,amountFor(p,m)-ps))}, обеды ${fmt(Math.max(0,amountFor(l,m)-ls))}. Каждую покупку и каждый обед заноси отдельной операцией — тогда помощник будет видеть реальный темп расходов.`,facts:[],actions:[]}}else if((r=categoryAnalysis(q))){}else if(/ближай|что.*оплат|платеж/.test(low)){const a=upcoming(14).filter(x=>x.kind!=='note').slice(0,12);r={answer:a.length?'На ближайшие 14 дней вижу:\n'+a.map(x=>`${ruDate(x.date,{day:'numeric',month:'short'})}: ${x.kind==='income'?'+':'−'}${fmt(x.amount)} — ${x.name}`).join('\n'):'На ближайшие 14 дней фиксированных событий нет.',facts:[],actions:[]}}else{const ni=nextIncome(state.selectedDate),rent=nextOccurrence('rent',state.selectedDate);r={answer:`Сейчас на руках ${fmt(state.currentBalance)}. ${ni?`Следующий фиксированный доход — ${fmt(ni.amount)} ${ruDate(ni.date,{day:'numeric',month:'long'})}. `:''}${rent?`Следующая квартира — ${fmt(rent.amount)} ${ruDate(rent.date,{day:'numeric',month:'long'})}. `:''}Я могу посчитать конкретный сценарий по дате и сумме — напиши его прямо в вопросе.`,facts:[],actions:[]}}const tx=proposedTransaction(q);if(tx)r.actions=(r.actions||[]).concat(tx);return r}
async function remoteAnswer(q,local){const endpoint=(state.aiEndpoint||'').trim()||(location.hostname.endsWith('vercel.app')?'/api/budget-assistant':'');if(!endpoint)return null;try{const res=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({question:q,snapshot:buildSnapshot(q),calculation:local})});if(!res.ok)throw new Error('HTTP '+res.status);const data=await res.json();return data.answer||data.text||null}catch(e){return null}}
function renderHome(){const ni=nextIncome(state.selectedDate);const un