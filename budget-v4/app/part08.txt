овая статья';$('catId').value=c?.id||'';$('catName').value=c?.name||'';$('catKind').value=c?.kind||'expense';$('catAmount').value=c?.amount||'';$('catSchedule').value=c?.schedule||'variable';$('catFrom').value=c?.from||monthOf(state.selectedDate);$('catDay').value=c?.day||'';$('catDayFrom').value=c?.dayFrom||'';$('catDayTo').value=c?.dayTo||'';$('catNote').value=c?.note||'';$('catOverrides').value=overridesText(c?.overrides);$('deleteCat').classList.toggle('hidden',!c);toggleCatSchedule();openModal('catModal')}function toggleCatSchedule(){const s=$('catSchedule').value;$('catDayField').classList.toggle('hidden',s!=='fixed');$('catRangeField').classList.toggle('hidden',s!=='range')}
function reverseEffect(t){if(t.affectsBalance===false||t.paidByOther)return;state.currentBalance+=(t.kind==='expense'?1:-1)*(Number(t.amount)||0)}function applyEffect(t){if(t.affectsBalance===false||t.paidByOther)return;state.currentBalance+=(t.kind==='income'?1:-1)*(Number(t.amount)||0)}
function appendMessage(role,text,meta='',facts=[],actions=[]){const wrap=document.createElement('div');wrap.className='msg-wrap '+(role==='user'?'user':'');wrap.innerHTML=`<div class="msg ${role==='user'?'user':'bot'}">${esc(text)}</div>${facts?.length?`<div class="analysis-box"><b>Что вошло в расчёт</b><ul>${facts.map(x=>`<li>${esc(x)}</li>`).join('')}</ul></div>`:''}${actions?.length?`<div class="action-row">${actions.map((a,i)=>`<button class="action-btn" data-ai-action="${i}">${esc(a.label)}</button>`).join('')}</div>`:''}${meta?`<div class="msg-meta">${esc(meta)}</div>`:''}`;wrap._actions=actions||[];$('chatLog').appendChild(wrap);$('chatLog').scrollTop=$('chatLog').scrollHeight;wrap.querySelectorAll('[data-ai-action]').forEach(b=>b.onclick=()=>applyAiAction(wrap._actions[Number(b.dataset.aiAction)],b))}
function applyAiAction(a,btn){if(!a)return;if(a.type==='override'){const c=category(a.categoryId);c.overrides=c.overrides||{};c.overrides[a.month]=a.amount;btn.textContent='Учтено ✓';btn.disabled=true;renderAll();return}if(a.type==='transaction'){const t={id:'t_'+Date.now(),kind:a.kind,amount:a.amount,date:a.date,categoryId:a.categoryId||'',name:a.name||'Операция',paidByOther:false,affectsBalance:true};state.transactions.push(t);applyEffect(t);btn.textContent='Учтено ✓';btn.disabled=true;renderAll()}}
async function ask(q){appendMessage('user',q);const local=smartLocal(q);appendMessage('bot',local.answer,'расчёт по данным приложения',local.facts,local.actions);const remote=await remoteAnswer(q,local);if(remote&&remote.trim()&&remote.trim()!==local.answer.trim())appendMessage('bot',remote,'GPT‑5.6 проверил расчёт и контекст')}
$('desktopNav').onclick=e=>{const b=e.target.closest('[data-screen]');if(b)showScreen(b.dataset.screen)};$('bottomNav').onclick=e=>{const b=e.target.closest('[data-screen]');if(b)showScreen(b.dataset.screen)};$('quickIncome').onclick=()=>openTx('income');$('quickExpense').onclick=()=>openTx('expense');$('histIncome').onclick=()=>openTx('income');$('histExpense').onclick=()=>openTx('expense');$('homeDate').onchange=e=>{state.selectedDate=e.target.value;renderAll()};$('prevDay').onclick=()=>{state.selectedDate=addDays(state.selectedDate,-1);renderAll()};$('nextDay').onclick=()=>{state.selectedDate=addDays(state.selectedDate,1);renderAll()};$('addPlannedToday').onclick=()=>{$('eventDate').value=state.selectedDate;$('eventAmount').value='';$('eventName').value='';$('eventKind').value='expense';$('eventPayer').value='me';openModal('eventModal')};$('planMonth').onchange=e=>{state.planMonth=e.target.value;renderAll()};$('addCategory').onclick=()=>openCat();$('categoriesTable').onclick=e=>{const b=e.target.closest('[data-cat]');if(b)openCat(category(b.dataset.cat))};$('historyTable').onclick=e=>{const b=e.target.closest('[data-edit-tx]');if(b)openTx('expense',state.transactions.find(t=>t.id===b.dataset.editTx))};document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>closeModal(b.dataset.close));$('txKind').onchange=e=>fillTxCategories(e.target.value);$('txMakeRecurring').onchange=toggleRecurring;$('recSchedule').onchange=toggleRecurring;$('catSchedule').onchange=toggleCatSchedule;
$('txForm').onsubmit=e=>{e.preventDefault();const id=$('txId').value,old=id?state.transactions.find(t=>t.id===id):null;if(old)reverseEffect(old);const t={id:id||'t_'+Date.now(),kind:$('txKind').value,amount:Number($('txAmount').value)||0,date:$('txDate').value,categoryId:$('txCategory').value,name:$('txName').value.trim(),paidByOther:$('txPaidByOther').checked,affectsBalance:true};if(id)state.transactions=state.transactions.map(x=>x.id===id?t:x);else state.transactions.push(t);applyEffect(t);if($('txMakeRecurring').checked&&!id){const s=$('recSchedule').value;state.categories.push({id:'c_'+Date.now(),name:$('recName').value.trim()||t.name,kind:t.kind,amount:t.amount,schedule:s,day:s==='fixed'?Number($('recDay').value)||1:undefined,dayFrom:s==='range'?Number($('recFrom').value)||1:undefined,dayTo:s==='range'?Number($('recTo').value)||1:undefined,from:monthOf(t.date),note:'Создано из операции',overrides:{}})}closeModal('txModal');renderAll()};$('deleteTx').onclick=()=>{const id=$('txId').value,t=state.transactions.find(x=>x.id===id);if(t&&confirm('Удалить операцию?')){reverseEffect(t);state.transactions=state.transactions.filter(x=>x.id!==id);closeModal('txModal');renderAll()}};
$('eventForm').onsubmit=e=>{e.preventDefault();state.plannedEvents.push({id:'p_'+Date.now(),date:$('eventDate').value,kind:$('eventKind').value,amount:Number($('eventAmount').value)||0,name:$('eventName').value.trim(),payer:$('eventPayer').value});closeModal('eventModal');renderAll()};
$('catForm').onsubmit=e=>{e.preventDefault();const id=$('catId').value,s=$('catSchedule').value,c={id:id||'c_'+Date.now(),name:$('catName').value.trim(),kind:$('catKind').value,amount:Number($('catAmount').value)||0,schedule:s,from:$('catFrom').value||null,note:$('catNote').value.trim(),overrides:parseOverrides($('catOverrides').value)};if(s==='fixed')c.day=Number($('catDay').value)||1;if(s==='range'){c.dayFrom=Number($('catDayFrom').value)||1;c.dayTo=Number($('catDayTo').value)||1}if(id)state.categories=state.categories.map(x=>x.id===id?c:x);else state.categories.push(c);closeModal('catModal');renderAll()};$('deleteCat').onclick=()=>{const id=$('catId').value;if(id&&confirm('Удалить статью? История останется.')){state.categories=state.categories.filter(x=>x.id!==id);closeModal('catModal');renderAll()}};
$('chatSend').onclick=()=>{const q=$('chatInput').value.trim();if(q){$('chatInput').value='';ask(q)}};$('chatInput').onkeydown=e=>{if(e.key==='Enter'&&!e.shiftKey){e.preventDefault();$('chatSend').click()}};document.querySelectorAll('.chip').forEach(b=>b.onclick=()=>ask(b.dataset.q));$('downloadJson').onclick=()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='budget-v4-backup.json';a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};$('uploadJson').onchange=e=>{const f=e.target.files[0];if(!f)return;const r=new FileReader();r.onload=()=>{try{state=normalize(JSON.parse(r.result));renderAll()}catch(err){alert('Не удалось прочитать JSON')}};r.readAsText(f)};$('resetAll').onclick=()=>{if(confirm('Вернуть исходные данные v4?')){state=clone(defaultState);renderAll()}};$('aiEndpoint').onchange=e=>{state.aiEndpoint=e.target.value.trim();renderAll()};
let deferredPrompt=null;window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredPrompt=e;$('installPwa').classList.add('visible')});$('installPwa').onclick=async()=>{if(!deferredPrompt)return;deferredPrompt.prompt();await deferredPrompt.userChoice;deferredPrompt=null;$('installPwa').classList.remove('visible')};window.addEventListener('appinstalled',()=>{$('installPwa').classList.remove('visible')});if('serviceWorker'in navigator)navigator.serviceWorker.register('./sw.js').catch(()=>{});
renderAll();
})();
</script>
</body></html>