/* MMT ДВИ runtime configuration. No secrets belong in this file. */
window.MMT_CONFIG = Object.assign({
  aiReviewEndpoint: 'https://mmt-dvi-review.mushkovmedia.workers.dev/'
}, window.MMT_CONFIG || {});
