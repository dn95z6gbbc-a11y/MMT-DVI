);const sav=Number(state.savings[m]||0);return {inc,exp,sav,free:inc-exp-sav}}
function monthList(){const a=[];for(let i=0;i<16;i++){const d=new Date(2026,8+i,1);a.push(`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`)}return a}
function renderHome(){document.getElementById('balanceValue').textContent=fmt(state.currentBalance);document.getElementById('balanceNote').textContent='Остаток вручную зафиксирован на '+dateObj(state.balanceAsOf).toLocaleDateString('ru-RU')+'. Новые операции меняют его автоматически.';document.getElementById('homeDate').value=state.selectedDate;document.getElementById('dayTitle').textContent=ruDate(state.selectedDate);const ni=nextIncome(state.selectedDate);const fixed=ni?fixedExpensesUntil(state.selectedDate,ni.date):0;const left=state.currentBalance-fixed;document.getElementById('situation').innerHTML=`<div class="mini"><span>Следующий фикс. доход</span><b>${ni?fmt(ni.amount):'—'}</b><small>${ni?dateObj(ni.date).toLocaleDateString('ru-RU'):'не найден'}</small></div><div class="mini"><span>Фикс. платежи до него</span><b>${fmt(fixed)}</b></div><div class="mini"><span>После них</span><b style="color:${left<0?'var(--red)':'var(--green)'}">${fmt(left)}</b></div>`;const rows=plannedDay(state.selectedDate);document.getElementById('dayList').innerHTML=rows.length?rows.map(x=>{const sign=x.kind==='income'?'+':x.kind==='expense'?'−':'';const cls=x.kind==='income'?'plus':x.kind==='expense'?'minus':'zero';const payer=x.payer==='other'?' · оплачивает другой человек':'';return `<div class="day-item"><div><strong>${esc(x.name)}</strong><small>${esc(x.note||'')}${payer}</small></div><div class="money ${cls}">${x.amount?sign+fmt(x.amount):'без суммы'}</div></div>`}).join(''):'<div class="empty">На этот день ничего не запланировано.</div>'}
function renderHistory(){const m=monthOf(state.selectedDate);const tx=txMonth(m).slice().sort((a,b)=>b.date.localeCompare(a.date));const inc=tx.filter(t=>t.kind==='income').reduce((s,t)=>s+Number(t.amount||0),0),exp=tx.filter(t=>t.kind==='expense').reduce((s,t)=>s+Number(t.amount||0),0),other=tx.filter(t=>t.paidByOther).reduce((s,t)=>s+Number(t.amount||0),0);document.getElementById('historyMetrics').innerHTML=`<div class="metric"><span>Доходы</span><b style="color:var(--green)">${fmt(inc)}</b></div><div class="metric"><span>Расходы</span><b style="color:var(--red)">${fmt(exp)}</b></div><div class="metric"><span>Оплатили другие</span><b>${fmt(other)}</b></div><div class="metric"><span>Операций</span><b>${tx.length}</b></div>`;document.getElementById('historyTable').innerHTML=tx.length?`<table><thead><tr><th>Дата</th><th>Операция</th><th>Категория</th><th>Кто платил</th><th class="num">Сумма</th><th></th></tr></thead><tbody>${tx.map(t=>`<tr><td>${dateObj(t.date).toLocaleDateString('ru-RU')}</td><td>${esc(t.name||'Без названия')}</td><td>${esc(category(t.categoryId)?.name||'—')}</td><td>${t.paidByOther?'Другой':'Я'}</td><td class="num ${t.kind==='income'?'plus':'minus'}">${t.kind==='income'?'+':'−'}${fmt(t.amount)}</td><td><button class="micro" data-edit-tx="${t.id}">изменить</button></td></tr>`).join('')}</tbody></table>`:'<div class="empty">Пока нет операций.</div>'}
function renderPlan(){const sel=document.getElementById('planMonth');if(!sel.options.length)sel.innerHTML=monthList().map(m=>`<option value="${m}">${dateObj(m+'-01').toLocaleDateString('ru-RU',{month:'long',year:'numeric'})}</option>`).join('');if(!monthList().includes(state.planMonth))state.planMonth='2026-09';sel.value=state.planMonth;const p=planMonth(state.planMonth);document.getElementById('planMetrics').innerHTML=`<div class="metric"><span>Доход</span><b style="color:var(--green)">${fmt(p.inc)}</b></div><div class="metric"><span>Расходы</span><b style="color:var(--red)">${fmt(p.exp)}</b></div><div class="metric"><span>В подушку</span><b style="color:var(--ochre)">${fmt(p.sav)}</b></div><div class="metric"><span>Свободно</span><b>${fmt(p.free)}</b></div>`;const inc=state.categories.filter(c=>c.kind==='income'&&amountFor(c,state.planMonth)),exp=state.categories.filter(c=>c.kind==='expense'&&amountFor(c,state.planMonth));const row=c=>`<div class="day-item"><div><strong>${esc(c.name)}</strong><small>${scheduleText(c)}${c.note?' · '+esc(c.note):''}</small></div><div class="money ${c.kind==='income'?'plus':'minus'}">${fmt(amountFor(c,state.planMonth))}</div></div>`;document.getElementById('planIncome').innerHTML=inc.map(row).join('');document.getElementById('planExpense').innerHTML=exp.map(row).join('')}
function renderCategories(){document.getElementById('categoriesTable').innerHTML=`<table><thead><tr><th>Статья</th><th>Тип</th><th>Когда</th><th class="num">Ориентир</th><th>Комментарий</th><th></th></tr></thead><tbody>${state.categories.map(c=>`<tr><td><b>${esc(c.name)}</b></td><td>${c.kind==='income'?'Доход':'Расход'}</td><td><span class="badge ${c.schedule==='variable'||c.schedule==='floating'?'variable':'fixed'}">${esc(scheduleText(c))}</span></td><td class="num">${fmt(c.amount)}</td><td>${esc(c.note||'')}</td><td><div class="cat-actions"><button class="micro" data-cat="${c.id}">изменить</button></div></td></tr>`).join('')}</tbody></table>`}
function renderAssistant(){const ni=nextIncome(state.selectedDate),fixed=ni?fixedExpensesUntil(state.selectedDate,ni.date):0;const prod=categorySpent('products',monthOf(state.selectedDate)),lunch=categorySpent('lunch',monthOf(state.selectedDate));document.getElementById('assistantSummary').innerHTML=`<h3 style="font:700 25px/1.1 Georgia,serif;margin-top:0">Сейчас</h3><div class="day-item"><div><strong>На руках</strong></div><div class="money">${fmt(state.currentBalance)}</div></div><div class="day-item"><div><strong>До следующего фиксированного дохода</strong><small>${ni?dateObj(ni.date).toLocaleDateString('ru-RU'):'—'}</small></div><div class="money">${ni?fmt(ni.amount):'—'}</div></div><div class="day-item"><div><strong>Фиксированные платежи до него</strong></div><div class="money minus">${fmt(fixed)}</div></div><div class="day-item"><div><strong>Продукты в этом месяце</strong></div><div class="money">${fmt(prod)} / ${fmt(category('products').amount)}</div></div><div class="day-item"><div><strong>Обеды в этом месяце</strong></div><div class="money">${fmt(lunch)} / ${fmt(category('lunch').amount)}</div></div>`}
function renderAll(){renderHome();renderHistory();renderPlan();renderCategories();renderAssistant();save()}
function openModal(id){document.getElementById(id).classList.add('open')}
function closeModal(id){document.getElementById(id).classList.remove('open')}
function fillTxCategories(kind){const sel=document.getElementById('txCategory');sel.innerHTML='<option value="">Без категории</option>'+state.categories.filter(c=>c.kind===kind).map(c=>`<option value="${c.id}">${esc(c.name)}</option>`).join('')}
function openTx(kind='expense',tx=null){document.getElementById('txTitle').textContent=tx?'Изменить операцию':kind==='income'?'Добавить доход':'Добавить расход';document.getElementById('txId').value=tx?.id||'';document.getElementById('txKind').value=tx?.kind||kind;fillTxCategories(tx?.kind||kind);document.getElementById('txAmount').value=tx?.amount||'';document.getElementById('txDate').value=tx?.date||state.selectedDate;document.getElementById('txCategory').value=tx?.categoryId||'';document.getElementById('txName').value=tx?.name||'';document.getElementById('txPaidByOther').checked=!!tx?.paidByOther;document.getElementById('txMakeRecurring').checked=false;document.getElementById('deleteTx').classList.toggle('hidden',!tx);toggleRecurring();openModal('txModal')}
function toggleRecurring(){const on=document.getElementById('txMakeRecurring').checked;['recNameField','recScheduleField'].forEach(id=>document.getElementById(id).classList.toggle('hidden',!on));document.getElementById('recDayField').classList.toggle('hidden',!on||document.getElementById('recSchedule').value!=='fixed');document.getElementById('recRangeField').classList.toggle('hidden',!on||document.getElementById('recSchedule').value!=='range')}
function parseOverrides(s){const o={};String(s||'').split(';').map(x=>x.trim()).filter(Boolean).forEach(p=>{const [k,v]=p.split('=').map(x=>x.trim());if(k&&v!==undefined)o[k]=Number(v)||0});return o}
function overridesText(o){return Object.entries(o||{}).map(([k,v])=>`${k}=${v}`).join('; ')}
function openCat(c=null){document.getElementById('catTitle').textContent=c?'Изменить категорию':'Новая категория';document.getElementById('catId').value=c?.id||'';document.getElementById('catName').value=c?.name||'';document.getElementById('catKind').value=c?.kind||'expense';document.getElementById('catAmount').value=c?.amount||'';document.getElementById('catSchedule').value=c?.schedule||'variable';document.getElementById('catFrom').value=c?.from||monthOf(state.selectedDate);document.getElementById('catDay').value=c?.day||'';document.getElementById('catDayFrom').value=c?.dayFrom||'';document.getElementById('catDayTo').value=c?.dayTo||'';document.getElementById('catNote').value=c?