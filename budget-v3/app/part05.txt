.note||'';document.getElementById('catOverrides').value=overridesText(c?.overrides);document.getElementById('deleteCat').classList.toggle('hidden',!c);toggleCatSchedule();openModal('catModal')}
function toggleCatSchedule(){const s=document.getElementById('catSchedule').value;document.getElementById('catDayField').classList.toggle('hidden',s!=='fixed');document.getElementById('catRangeField').classList.toggle('hidden',s!=='range')}
function reverseEffect(t){if(t.affectsBalance===false)return;if(t.paidByOther)return;state.currentBalance+=(t.kind==='expense'?1:-1)*(Number(t.amount)||0)}
function applyEffect(t){if(t.affectsBalance===false)return;if(t.paidByOther)return;state.currentBalance+=(t.kind==='income'?1:-1)*(Number(t.amount)||0)}

document.getElementById('tabs').onclick=e=>{const b=e.target.closest('.tab');if(!b)return;document.querySelectorAll('.tab').forEach(x=>x.classList.remove('active'));document.querySelectorAll('.screen').forEach(x=>x.classList.remove('active'));b.classList.add('active');document.getElementById('screen-'+b.dataset.screen).classList.add('active');renderAll()};
document.getElementById('quickIncome').onclick=()=>openTx('income');document.getElementById('quickExpense').onclick=()=>openTx('expense');document.getElementById('histIncome').onclick=()=>openTx('income');document.getElementById('histExpense').onclick=()=>openTx('expense');
document.getElementById('editBalance').onclick=()=>{const v=prompt('Сколько денег у тебя сейчас в наличии?',String(state.currentBalance));if(v!==null&&!isNaN(Number(v))){state.currentBalance=Number(v);state.balanceAsOf=state.selectedDate;renderAll()}};
document.getElementById('homeDate').onchange=e=>{state.selectedDate=e.target.value;renderAll()};document.getElementById('prevDay').onclick=()=>{state.selectedDate=addDays(state.selectedDate,-1);renderAll()};document.getElementById('nextDay').onclick=()=>{state.selectedDate=addDays(state.selectedDate,1);renderAll()};
document.getElementById('addPlannedToday').onclick=()=>{document.getElementById('eventDate').value=state.selectedDate;document.getElementById('eventAmount').value='';document.getElementById('eventName').value='';document.getElementById('eventKind').value='expense';document.getElementById('eventPayer').value='me';openModal('eventModal')};
document.getElementById('addCategory').onclick=()=>openCat();document.getElementById('catSchedule').onchange=toggleCatSchedule;document.getElementById('txKind').onchange=e=>fillTxCategories(e.target.value);document.getElementById('txMakeRecurring').onchange=toggleRecurring;document.getElementById('recSchedule').onchange=toggleRecurring;
document.querySelectorAll('[data-close]').forEach(b=>b.onclick=()=>closeModal(b.dataset.close));
document.getElementById('txForm').onsubmit=e=>{e.preventDefault();const id=document.getElementById('txId').value;let old=id?state.transactions.find(t=>t.id===id):null;if(old)reverseEffect(old);const t={id:id||'t_'+Date.now(),kind:document.getElementById('txKind').value,amount:Number(document.getElementById('txAmount').value)||0,date:document.getElementById('txDate').value,categoryId:document.getElementById('txCategory').value,name:document.getElementById('txName').value.trim(),paidByOther:document.getElementById('txPaidByOther').checked,affectsBalance:true};if(id){state.transactions=state.transactions.map(x=>x.id===id?t:x)}else state.transactions.push(t);applyEffect(t);if(document.getElementById('txMakeRecurring').checked&&!id){const sched=document.getElementById('recSchedule').value;state.categories.push({id:'c_'+Date.now(),name:document.getElementById('recName').value.trim()||t.name||'Новая статья',kind:t.kind,amount:t.amount,schedule:sched,day:sched==='fixed'?Number(document.getElementById('recDay').value)||1:undefined,dayFrom:sched==='range'?Number(document.getElementById('recFrom').value)||1:undefined,dayTo:sched==='range'?Number(document.getElementById('recTo').value)||1:undefined,from:monthOf(t.date),note:'Создано из операции',overrides:{}})}closeModal('txModal');renderAll()};
document.getElementById('deleteTx').onclick=()=>{const id=document.getElementById('txId').value,t=state.transactions.find(x=>x.id===id);if(t&&confirm('Удалить операцию?')){reverseEffect(t);state.transactions=state.transactions.filter(x=>x.id!==id);closeModal('txModal');renderAll()}};
document.getElementById('catForm').onsubmit=e=>{e.preventDefault();const id=document.getElementById('catId').value,s=document.getElementById('catSchedule').value;const c={id:id||'c_'+Date.now(),name:document.getElementById('catName').value.trim(),kind:document.getElementById('catKind').value,amount:Number(document.getElementById('catAmount').value)||0,schedule:s,from:document.getElementById('catFrom').value||null,note:document.getElementById('catNote').value.trim(),overrides:parseOverrides(document.getElementById('catOverrides').value)};if(s==='fixed')c.day=Number(document.getElementById('catDay').value)||1;if(s==='range'){c.dayFrom=Number(document.getElementById('catDayFrom').value)||1;c.dayTo=Number(document.getElementById('catDayTo').value)||1}if(id)state.categories=state.categories.map(x=>x.id===id?c:x);else state.categories.push(c);closeModal('catModal');renderAll()};
document.getElementById('deleteCat').onclick=()=>{const id=document.getElementById('catId').value;if(id&&confirm('Удалить категорию? История операций останется.')){state.categories=state.categories.filter(x=>x.id!==id);closeModal('catModal');renderAll()}};
document.getElementById('eventForm').onsubmit=e=>{e.preventDefault();state.plannedEvents.push({id:'p_'+Date.now(),date:document.getElementById('eventDate').value,kind:document.getElementById('eventKind').value,amount:Number(document.getElementById('eventAmount').value)||0,name:document.getElementById('eventName').value.trim(),payer:document.getElementById('eventPayer').value});closeModal('eventModal');renderAll()};
document.getElementById('categoriesTable').onclick=e=>{const b=e.target.closest('[data-cat]');if(b)openCat(category(b.dataset.cat))};document.getElementById('historyTable').onclick=e=>{const b=e.target.closest('[data-edit-tx]');if(b)openTx('expense',state.transactions.find(t=>t.id===b.dataset.editTx))};
document.getElementById('planMonth').onchange=e=>{state.planMonth=e.target.value;renderPlan();save()};
function localAnswer(q){q=q.toLowerCase();const ni=nextIncome(state.selectedDate),fixed=ni?fixedExpensesUntil(state.selectedDate,ni.date):0,left=state.currentBalance-fixed;const prod=categorySpent('products',monthOf(state.selectedDate)),lunch=categorySpent('lunch',monthOf(state.selectedDate));if(q.includes('сколько')&&q.includes('потрат'))return ni?`До ${dateObj(ni.date).toLocaleDateString('ru-RU')} у тебя сейчас ${fmt(state.currentBalance)}. Фиксированные платежи до этого дня — ${fmt(fixed)}. Формально остаётся ${fmt(left)}. Переменные покупки сюда не включены.`:`Сейчас у тебя ${fmt(state.currentBalance)}. Следующий фиксированный доход в горизонте 60 дней не найден.`;if(q.includes('ближай')||q.includes('платеж')){let a=[];for(let i=0;i<14;i++){const d=addDays(state.selectedDate,i);plannedDay(d).filter(x=>x.kind!=='note').forEach(x=>a.push(`${dateObj(d).toLocaleDateString('ru-RU',{day:'numeric',month:'short'})}: ${x.kind==='income'?'+':'−'}${fmt(x.amount)} — ${x.name}`))}return a.length?a.slice(0,8).join('\n'):'На ближайшие 14 дней фиксированных событий нет.'}if(q.includes('продукт')||q.includes('обед'))return `В этом месяце по продуктам внесено ${fmt(prod)} из ориентира ${fmt(category('products').amount)}. По обедам — ${fmt(lunch)} из ${fmt(category('lunch').amount)}. Каждую покупку и каждый обед лучше заносить отдельной операцией.`;if(q.includes('15 сентября')||q.includes('хватит'))return ni?`До ближайшего фиксированного дохода ${dateObj(ni.date).toLocaleDateString('ru-RU')} приложение видит ${fmt(fixed)} фиксированных платежей. При текущем остатке после них останется ${fmt(left)}. Помощь родителей, если она не требует твоих денег, можно записывать с отметкой «оплачивает другой человек».`:`Сейчас на руках ${fmt(state.currentBalance)}.`;return `Сейчас у тебя ${fmt(state.currentBalance)}. Ближайший фиксированный доход: ${ni?fmt(ni.amount)+' '+dateObj(ni.date).toLocaleDateString('ru-RU'):'не найден'}. Спроси про ближайшие платежи, продукты, обеды или сколько можно потратить.`}
function ask(q){const log=document.getElementById('chatLog');log.insertAdjacentHTML('beforeend',`<div class="msg user">${esc(q)}</div><div class="msg bot">${esc(localAnswer(q))}</div>`);log.scrollTop=log.scrollHeight}
document.getElementById('chatSend').onclick=()=>{const i=document.getElementById('chatInput'),q=i.value.trim();if(q){ask(q);i.value=''}};document.getElementById('chatInput').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();document.getElementById('chatSend').click()}};document.querySelectorAll('.chip').forEach(b=>b.onclick=()=>ask(b.dataset.q));
document.getElementById('downloadJson').onclick=()=>{const blob=new Blob([JSON.stringify(state,null,2)],{type:'application/json'}),a=