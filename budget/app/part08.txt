</div><div class="ai-note">В онлайн-режиме приложению нужен интернет. Ключ намеренно не сохраняется. Если браузер блокирует запросы из локального HTML, можно указать свой совместимый прокси-endpoint; локальный помощник продолжит работать независимо.</div></details><div class="chat"><div class="chat-log" id="chatLog"><div class="msg bot">Спросите, например: «Могу ли я потратить 12 000 ₽?», «Что изменилось после новых расходов?» или «Почему в этом месяце мало свободных денег?»</div></div><div class="chips"><button class="chip">Могу ли я потратить 12 000 ₽?</button><button class="chip">Что изменилось?</button><button class="chip">Что ещё обязательно оплатить?</button></div><div class="chat-input"><input id="chatInput" placeholder="Ваш вопрос про бюджет"><button class="primary" id="chatSend">Спросить</button></div><div class="status-line" id="aiStatus"></div></div>`;
 const key=document.getElementById('aiKey'),model=document.getElementById('aiModel'),endpoint=document.getElementById('aiEndpoint'),mode=document.getElementById('aiMode');key.oninput=e=>{apiKeySession=e.target.value};model.onchange=e=>{state.ai.model=e.target.value.trim();save()};endpoint.onchange=e=>{state.ai.endpoint=e.target.value.trim();save()};mode.onchange=e=>{state.ai.online=e.target.value==='online';save();renderChat()};
 const log=document.getElementById('chatLog'),inp=document.getElementById('chatInput'),status=document.getElementById('aiStatus');async function ask(text){const q=(text||inp.value).trim();if(!q)return;log.insertAdjacentHTML('beforeend',`<div class="msg user">${esc(q)}</div>`);inp.value='';if(state.ai.online){status.textContent='Запрашиваю модель…';document.getElementById('chatSend').disabled=true;try{const ans=await onlineAnswer(q);log.insertAdjacentHTML('beforeend',`<div class="msg bot">${esc(ans)}</div>`);status.textContent='Ответ получен.'}catch(e){log.insertAdjacentHTML('beforeend',`<div class="msg error">Онлайн-ИИ: ${esc(e.message)}\n\nЛокальный ответ: ${esc(localAnswer(q))}</div>`);status.textContent='Онлайн-запрос не удался; показан локальный ответ.'}finally{document.getElementById('chatSend').disabled=false}}else{log.insertAdjacentHTML('beforeend',`<div class="msg bot">${esc(localAnswer(q))}</div>`)}log.scrollTop=log.scrollHeight}
 document.getElementById('chatSend').onclick=()=>ask();inp.onkeydown=e=>{if(e.key==='Enter')ask()};document.querySelectorAll('#chatCard .chip').forEach(b=>b.onclick=()=>ask(b.textContent))}
function renderAssistant(){renderToday();renderWarnings();renderActions();renderScenario();renderChat()}

function renderPlan(){const months=monthList(),income=state.cats.filter(c=>c.kind==='income'),expense=state.cats.filter(c=>c.kind==='expense');let cum=0;const row=c=>`<tr><td>${esc(c.name)}</td>${months.map(m=>`<td class="${c.kind}">${amountFor(c,m)?fmt(amountFor(c,m)):''}</td>`).join('')}</tr>`;const savingRow=`<tr class="saving-row"><td>Отчисление в подушку</td>${months.map(m=>`<td><input class="plan-input" type="number" step="500" data-saving-month="${m}" value="${Number(state.savings[m]||0)}"></td>`).join('')}</tr>`;const cumVals=months.map(m=>{cum+=Number(state.savings[m]||0);return cum});const summary=(label,klass,fn)=>`<tr class="${klass}"><td>${label}</td>${months.map(m=>`<td>${fmt(fn(m))}</td>`).join('')}</tr>`;document.getElementById('planTable').innerHTML=`<table><thead><tr><th>Статья</th>${months.map(m=>`<th>${monthShort(m)}</th>`).join('')}</tr></thead><tbody><tr class="section-row"><td colspan="${months.length+1}">Доходы · план</td></tr>${income.map(row).join('')}<tr class="section-row"><td colspan="${months.length+1}">Расходы · план</td></tr>${expense.map(row).join('')}<tr class="section-row"><td colspan="${months.length+1}">Подушка</td></tr>${savingRow}<tr class="cum-row"><td>Накоплено по плану</td>${cumVals.map(v=>`<td>${fmt(v)}</td>`).join('')}</tr><tr class="section-row"><td colspan="${months.length+1}">План / факт / прогноз</td></tr>${summary('Свободно · исходный план','',m=>monthPlan(m).free)}${summary('Факт · получено','fact-row',m=>actualData(m).income)}${summary('Факт · потрачено','fact-row',m=>actualData(m).expense)}${summary('Незапланированный эффект','fact-row',m=>unplannedImpact(m))}${summary('Свободно · прогноз','forecast-row',m=>forecastData(m).free)}</tbody></table>`;document.querySelectorAll('[data-saving-month]').forEach(inp=>inp.onchange=e=>{state.savings[e.target.dataset.savingMonth]=Number(e.target.value)||0;save();renderAll()})}
function overridesToString(o){return Object.entries(o||{}).map(([k,v])=>`${k}=${v}`).join(', ')}
function parseOverrides(s){const o={};String(s||'').split(',').map(x=>x.trim()).filter(Boolean).forEach(p=>{const [k,v]=p.split('=').map(x=>x.trim());if(/^\d{4}-\d{2}$/.test(k)&&v!==undefined&&!Number.isNaN(Number(v)))o[k]=Number(v)});return o}
function renderCategories(){const rows=state.cats.map((c,i)=>`<tr data-cat-index="${i}"><td><input data-field="name" value="${esc(c.name)}"></td><td><select data-field="kind"><option value="income" ${c.kind==='income'?'selected':''}>доход</option><option value="expense" ${c.kind==='expense'?'selected':''}>расход</option></select></td><td><input data-field="amount" type="number" step="100" value="${c.amount}"></td><td><select data-field="freq"><option value="month" ${c.freq==='month'?'selected':''}>в месяц</option><option value="week" ${c.freq==='week'?'selected':''}>в неделю</option><option value="once" ${c.freq==='once'?'selected':''}>разово</option></select></td><td><input data-field="day" value="${c.day==='spread'?'равномерно':c.day}"></td><td><input data-field="from" placeholder="YYYY-MM" value="${c.from||''}"></td><td><input data-field="months" placeholder="2027-03, 2027-06" value="${(c.months||[]).join(', ')}"></td><td><input data-field="overrides" placeholder="2026-09=22000" value="${esc(overridesToString(c.overrides))}"></td><td><button class="remove-btn" data-remove="${i}" title="Удалить">×</button></td></tr>`).join('');document.getElementById('categoriesTable').innerHTML=`<table class="cats-table"><thead><tr><th>Название</th><th>Тип</th><th>Сумма</th><th>Периодичность</th><th>День</th><th>Начало</th><th>Разовые месяцы</th><th>Исключения</th><th></th></tr></thead><tbody>${rows}</tbody></table>`;document.querySelectorAll('#categoriesTable [data-field]').forEach(el=>el.onchange=e=>{const tr=e.target.closest('tr'),i=Number(tr.dataset.catIndex),field=e.target.dataset.field,c=state.cats[i];if(field==='amount')c.amount=Number(e.target.value)||0;else if(field==='day'){const v=e.target.value.trim().toLowerCase();c.day=(v==='равномерно'||v==='spread')?'spread':Math.min(31,Math.max(1,Number(v)||1))}else if(field==='months')c.months=e.target.value.split(',').map(x=>x.trim()).filter(Boolean);else if(field==='overrides')c.overrides=parseOverrides(e.target.value);else if(field==='from')c.from=e.target.value.trim()||null;else c[field]=e.target.value;save();renderAll()});document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{const i=Number(b.dataset.remove),c=state.cats[i];if(state.transactions.some(t=>t.categoryId===c.id)&&!confirm('Есть фактические операции, связанные с этой категорией. Удалить категорию всё равно?'))return;state.cats.splice(i,1);save();renderAll()})}

function fillCategoryOptions(kind,selected=''){const opts=state.cats.filter(c=>c.kind===kind).map(c=>`<option value="${esc(c.id)}" ${c.id===selected?'selected':''}>${esc(c.name)}</option>`).join('');document.getElementById('opCategory').innerHTML='<option value="">Без категории</option>'+opts}
function openOperation(opts={}){const existing=opts.id?state.transactions.find(t=>t