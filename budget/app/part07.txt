st t=q.toLowerCase(),m=state.selectedMonth,p=monthPlan(m),a=actualData(m),f=forecastData(m),u=unplannedImpact(m),c=chronic(),cum=cumulativeSavings(),end=monthList().at(-1),money=parseMoneyFromQuestion(q);if(/куп|потрат|ремонт|заплат|могу ли|хватит/.test(t)&&money&&money>100){const after=f.free-money;return `Если добавить расход ${fmt(money)} прямо сейчас, прогноз свободного остатка станет ${fmt(after)}. ${after<0?'Это ломает текущий план на '+fmt(Math.abs(after))+'. Нужен перенос, дополнительный доход или уменьшение другой статьи/подушки.':after<3000?'Формально план сходится, но запас останется меньше 3 000 ₽.':'План по текущим цифрам выдерживает этот расход.'}`}
 if(/сегодня|лимит|сколько.*трат/.test(t)){const days=daysInMonth(m),spread=spreadTotal(m),actualSpread=actualSpreadExpense(m);return `По выбранному месяцу равномерный бюджет — ${fmt(spread)}. Фактом по таким категориям внесено ${fmt(actualSpread)}. Свободно сверх всего плана с учётом отклонений — ${fmt(f.free)}.`}
 if(/что измен|почему.*мало|отклон/.test(t))return `От исходного плана прогноз изменился на ${fmt(f.free-p.free,true)}: незапланированные операции ${fmt(u,true)}, отклонения по закрытым плановым статьям ${fmt(f.variance,true)}. Сейчас прогноз свободного остатка — ${fmt(f.free)}.`
 if(/70|семьдесят|210|двести десять|ступ/.test(t))return `По плану 70 000 ₽: ${milestone(cum,70000)?monthLabel(milestone(cum,70000)):'не набираются в горизонте'}. 210 000 ₽: ${milestone(cum,210000)?monthLabel(milestone(cum,210000)):'не набираются в горизонте'}. К концу горизонта — ${fmt(cum[end])}.`
 if(/первая|начал|квартир|резерв/.test(t))return `Первая половина структурно напряжённая: хронический минимум ${fmt(c.min)}. Рекомендуемый неснижаемый остаток — около ${fmt(c.recommended)}. Для выбранного месяца безопасный резерв на 30-е — ${fmt(recommendedReserve(m))}.`
 if(/сократ|эконом|убрать|что если/.test(t))return `Готовые рычаги: сигареты ≈ 6 933 ₽/мес, обеды 3 раза вместо 5 ≈ 4 333 ₽/мес, одна нейросеть +3 000 ₽/мес, Контур Толк +1 400 ₽/мес, второй ученик ≈ +7 680 ₽/мес после дополнительного налога.`
 if(/подуш|накоп/.test(t))return `В ${monthLabel(m).toLowerCase()} плановый перевод в подушку — ${fmt(p.saving)}. По плану накоплено к концу месяца ${fmt(cum[m])}, к декабрю 2027 — ${fmt(cum[end])}. Фактические операции сами подушку не меняют, пока вы не отредактируете отчисление.`
 if(/обяз|ещ[её].*оплат|что.*платить|платеж/.test(t)){const rem=remainingFixed(m,'expense');if(!rem.length)return `Все фиксированные плановые расходы ${monthLabel(m).toLowerCase()} уже отмечены фактом. Равномерные траты и подушка учитываются отдельно.`;const total=rem.reduce((s,x)=>s+x.amount,0),top=rem.slice(0,5).map(x=>`${x.day}-го ${x.name} — ${fmt(x.amount)}`).join('; ');return `Ещё не отмечены фактом фиксированные расходы на ${fmt(total)}. Ближайшие: ${top}${rem.length>5?' и ещё '+(rem.length-5):''}. Плюс остаются равномерные траты и плановый перевод в подушку ${fmt(p.saving)}.`}
 if(/факт|реально|получ|потрач/.test(t))return `Внесённый факт за ${monthLabel(m).toLowerCase()}: получено ${fmt(a.income)}, потрачено ${fmt(a.expense)}, операций ${a.count}. Это не банковская выписка: учитывается только то, что вы внесли в приложение.`
 if(/вклад|кредит|инвест/.test(t))return `Я могу посчитать нагрузку и последствия для бюджета, но решение по вкладам, кредитам и инвестициям остаётся за вами. Сейчас прогноз свободного остатка выбранного месяца — ${fmt(f.free)}.`
 return `По ${monthLabel(m).toLowerCase()}: план доходов ${fmt(p.income)}, расходов ${fmt(p.expense)}, подушка ${fmt(p.saving)}. Фактом внесено: +${fmt(a.income)} и −${fmt(a.expense)}. Свободно по прогнозу — ${fmt(f.free)}. Можно спросить: «Могу ли я потратить 12 000?», «Что изменилось?» или «Почему осталось мало?»`}
function aiContext(m){const p=monthPlan(m),a=actualData(m),f=forecastData(m),cum=cumulativeSavings();return {currentDate:new Date().toISOString().slice(0,10),selectedMonth:m,monthLabel:monthLabel(m),plan:{income:Math.round(p.income),expense:Math.round(p.expense),saving:Math.round(p.saving),free:Math.round(p.free),firstHalf:Math.round(p.first.balance),secondHalf:Math.round(p.second.balance)},actual:{income:Math.round(a.income),expense:Math.round(a.expense),count:a.count},forecast:{free:Math.round(f.free),unplannedImpact:Math.round(f.unplanned),plannedVariance:Math.round(f.variance)},plannedItems:state.cats.map(c=>({id:c.id,name:c.name,kind:c.kind,amount:Math.round(amountFor(c,m)),day:c.day,freq:c.freq,settled:isSettled(c,m)})).filter(x=>x.amount!==0),transactions:txForMonth(m).map(t=>({date:t.date,name:t.name,kind:t.kind,amount:Number(t.amount),mode:t.mode,category:categoryById(t.categoryId)?.name||null,note:t.note||''})),savingsSchedule:monthList().map(mm=>({month:mm,deposit:Number(state.savings[mm]||0),cumulative:Math.round(cum[mm])})),emergencyTargets:[70000,210000,408000],recommendedMinimumBalance:chronic().recommended,safeReserveFor30th:Math.round(recommendedReserve(m))}}
async function onlineAnswer(q){if(!apiKeySession)throw new Error('Сначала введите API-ключ в настройках ИИ.');const endpoint=(state.ai.endpoint||'').trim(),model=(state.ai.model||'').trim();if(!endpoint||!model)throw new Error('Заполните endpoint и модель.');const instructions='Ты встроенный помощник приложения личного бюджета. Отвечай по-русски, коротко и конкретно. Используй только данные из переданного JSON; не придумывай отсутствующие цифры. Различай план и внесённый факт. Не морализируй. Если вопрос про вклад, кредит или инвестиции, можешь посчитать последствия, но скажи, что решение остаётся за пользователем. Если данных недостаточно, прямо скажи, чего не хватает.';const body={model,instructions,input:`Вопрос пользователя: ${q}\n\nСлепок бюджета:\n${JSON.stringify(aiContext(state.selectedMonth))}`};const r=await fetch(endpoint,{method:'POST',headers:{'Content-Type':'application/json','Authorization':'Bearer '+apiKeySession},body:JSON.stringify(body)});let data=null;try{data=await r.json()}catch(e){}if(!r.ok)throw new Error((data&&data.error&&data.error.message)||`Ошибка API: ${r.status}`);if(data&&typeof data.output_text==='string'&&data.output_text.trim())return data.output_text.trim();const parts=[];(data&&data.output||[]).forEach(item=>(item.content||[]).forEach(c=>{if(typeof c.text==='string')parts.push(c.text)}));if(parts.length)return parts.join('\n').trim();throw new Error('Модель не вернула текстовый ответ.')}
function renderChat(){const online=!!state.ai.online;document.getElementById('chatCard').innerHTML=`<div class="card-head"><div><h3>ИИ-помощник</h3><p>${online?'Онлайн-режим: полный слепок выбранного месяца передаётся модели.':'Локальный режим: работает без интернета и никуда не отправляет данные.'}</p></div><span class="pill ${online?'online':'offline'}">${online?'онлайн':'офлайн'}</span></div><details class="ai-settings"><summary>Настройки настоящего ИИ</summary><div class="ai-settings-grid"><div class="field full"><label>API-ключ (только на время открытой страницы, не сохраняется в JSON/localStorage)</label><input id="aiKey" type="password" placeholder="Вставьте ключ" value="${esc(apiKeySession)}"></div><div class="field"><label>Модель</label><input id="aiModel" value="${esc(state.ai.model||'gpt-5.6-luna')}"></div><div class="field"><label>Режим</label><select id="aiMode"><option value="local" ${!online?'selected':''}>Локальный</option><option value="online" ${online?'selected':''}>Онлайн через API</option></select></div><div class="field full"><label>Endpoint</label><input id="aiEndpoint" value="${esc(state.ai.endpoint||'https://api.openai.com/v1/responses')}"></div>