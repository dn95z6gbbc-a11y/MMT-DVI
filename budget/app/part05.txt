ll){let total=0,out={};monthList().forEach(m=>{total+=Number(state.savings[m]||0);if(extraPerMonth&&(!fromMonth||m>=fromMonth))total+=extraPerMonth;out[m]=total});return out}
function currentMonthInHorizon(){const d=new Date(),m=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`,list=monthList();return list.includes(m)?m:(m<list[0]?list[0]:list[list.length-1])}
function activeScenarioEffect(){let e=0;if(state.levers.kontur)e+=1400;if(state.levers.oneAi)e+=3000;if(state.levers.lunch3)e+=2*500*4.333;if(state.levers.secondStudent)e+=8000-320;if(state.levers.quitSmoking)e+=1600*4.333;return e}
function milestone(cum,target){for(const m of monthList())if(cum[m]>=target)return m;return null}
function daysInMonth(m){const [y,mo]=m.split('-').map(Number);return new Date(y,mo,0).getDate()}
function defaultDateForMonth(m){const now=new Date(),cur=`${now.getFullYear()}-${String(now.getMonth()+1).padStart(2,'0')}`;if(m===cur)return `${m}-${String(now.getDate()).padStart(2,'0')}`;return `${m}-01`}
function categoryById(id){return state.cats.find(c=>c.id===id)||null}

function initMonthSelect(){const sel=document.getElementById('monthSelect');sel.innerHTML=monthList().map(m=>`<option value="${m}">${monthLabel(m)}</option>`).join('');if(!monthList().includes(state.selectedMonth))state.selectedMonth=monthList()[0];sel.value=state.selectedMonth}
function renderHalf(which,data,m){const title=which==='first'?'Первая половина':'Вторая половина',subtitle=which==='first'?'30-е → 14-е':'15-е → 29-е';const incomes=data.rows.filter(r=>r.cat.kind==='income'),expenses=data.rows.filter(r=>r.cat.kind==='expense');
 const row=r=>{const c=r.cat,amountText=(c.kind==='income'?'+':'−')+fmt(Math.abs(r.amount));return `<div class="money-row"><div>${esc(c.name)}${r.spread?'<span class="meta">(половина равномерной статьи)</span>':`<span class="meta">${c.day}-го${r.settled?' · факт внесён':''}</span>`}</div><div class="amount ${c.kind}">${amountText}</div><div class="row-actions"><button class="micro" data-edit-plan="${esc(c.id)}">изменить</button><button class="micro ${r.settled?'settled':''}" data-plan-fact="${esc(c.id)}">${r.settled?'факт ✓':'учесть факт'}</button></div></div>`};
 return `<div class="card"><div class="card-head"><div><h2>${title}</h2><p>${subtitle}</p></div><span class="pill">${data.rows.length} статей</span></div><div class="list">${incomes.map(row).join('')}${expenses.map(row).join('')}${data.saving!==0?`<div class="money-row"><div>В подушку<span class="meta">${state.savingDay}-го</span></div><div class="amount saving">${data.saving>=0?'−':'+'}${fmt(Math.abs(data.saving))}</div><div></div></div>`:''}</div><div class="subtotal"><span>План: останется</span><strong class="${data.balance<0?'negative':'positive'}">${fmt(data.balance)}</strong></div></div>`}
function renderMonth(){const m=state.selectedMonth,p=monthPlan(m),a=actualData(m),f=forecastData(m);document.getElementById('halfCards').innerHTML=renderHalf('first',p.first,m)+renderHalf('second',p.second,m);document.getElementById('monthMetrics').innerHTML=`
 <div class="metric"><div class="label">План · доход</div><div class="value income">${fmt(p.income)}</div></div>
 <div class="metric"><div class="label">План · расходы</div><div class="value expense">${fmt(p.expense)}</div></div>
 <div class="metric"><div class="label">Факт · получено</div><div class="value actual">${fmt(a.income)}</div><small>только внесённые операции</small></div>
 <div class="metric"><div class="label">Факт · потрачено</div><div class="value actual">${fmt(a.expense)}</div><small>только внесённые операции</small></div>
 <div class="metric"><div class="label">В подушку</div><input id="monthSavingInput" type="number" step="500" value="${p.saving}"></div>
 <div class="metric"><div class="label">Свободно по прогнозу</div><div class="value ${f.free<0?'negative':'positive'}">${fmt(f.free)}</div><small>план + отклонения факта</small></div>`;
 document.getElementById('monthSavingInput').onchange=e=>{state.savings[m]=Number(e.target.value)||0;save();renderAll()};
 document.querySelectorAll('[data-edit-plan]').forEach(b=>b.onclick=()=>editPlanAmount(b.dataset.editPlan,m));document.querySelectorAll('[data-plan-fact]').forEach(b=>b.onclick=()=>openPlannedFact(b.dataset.planFact,m));renderChangeCard()}
function editPlanAmount(catId,m){const c=categoryById(catId);if(!c)return;const current=amountFor(c,m),raw=prompt(`Сумма «${c.name}» в ${monthLabel(m).toLowerCase()}.\nВведите новую сумму. Пустое поле вернёт базовое значение ${fmt(c.freq==='week'?c.amount: c.amount)}.`,String(Math.round(current)));if(raw===null)return;if(raw.trim()===''){if(c.overrides)delete c.overrides[m]}else{const n=Number(raw.replace(/\s/g,'').replace(',','.'));if(!Number.isFinite(n)||n<0){alert('Нужна сумма числом.');return}c.overrides=c.overrides||{};c.overrides[m]=n}save();renderAll()}

function operationEffect(t,m){if(t.mode==='unplanned')return (t.kind==='income'?1:-1)*Number(t.amount||0);const c=categoryById(t.categoryId);if(!c)return 0;if(c.day==='spread')return 0;const all=txByCat(m,c.id,'planned'),sum=txSum(all),planned=amountFor(c,m);if(c.kind==='income')return sum-planned;return planned-sum}
function renderChangeCard(){const m=state.selectedMonth,p=monthPlan(m),f=forecastData(m),delta=f.free-p.free,last=state.transactions.find(t=>t.id===state.lastChangeId&&monthOfDate(t.date)===m),ops=txForMonth(m).slice().sort((a,b)=>b.date.localeCompare(a.date)||String(b.createdAt).localeCompare(String(a.createdAt))).slice(0,5);let text='Факт пока не изменил прогноз месяца.';if(delta<0)text=`Фактические операции ухудшили прогноз на ${fmt(Math.abs(delta))}.`;if(delta>0)text=`Фактические операции улучшили прогноз на ${fmt(delta)}.`;if(last){const sign=last.kind==='income'?'+':'−';text+=` Последнее изменение: ${sign}${fmt(last.amount)} · ${last.name}.`}document.getElementById('changeCard').innerHTML=`<div class="card-head"><div><h3>Что изменилось?</h3><p>Сравнение исходного плана с тем, что уже произошло</p></div><span class="pill ${delta<0?'offline':'online'}">${delta===0?'без изменений':fmt(delta,true)}</span></div><div class="change-main"><div class="change-text">${esc(text)}<div class="muted" style="margin-top:7px">Плановый свободный остаток: ${fmt(p.free)} · текущий прогноз: <b>${fmt(f.free)}</b>.</div></div><div><span class="muted">Дополнительный запас</span><strong class="${f.free<0?'negative':'positive'}">${fmt(f.free)}</strong></div></div>${ops.length?`<div style="margin-top:14px">${ops.map(t=>`<div class="op-preview"><div class="op-date">${new Date(t.date+'T12:00:00').toLocaleDateString('ru-RU',{day:'2-digit',month:'2-digit'})}</div><div>${esc(t.name)}<span class="tag ${t.mode==='unplanned'?'unplanned':''}">${t.mode==='unplanned'?'незаплан.':'по плану'}</span></div><div class="amount ${t.kind}">${t.kind==='income'?'+':'−'}${fmt(t.amount)}</div></div>`).join('')}</div>`:'<div class="muted" style="margin-top:14px">За этот месяц фактических операций ещё нет.</div>'}`}

function renderFact(){const m=state.selectedMonth,a=actualData(m),u=unplannedImpact(m),f=forecastData(m),ops=txForMonth(m).slice().sort((a,b)=>b.date.localeCompare(a.date)||String(b.createdAt).localeCompare(String(a.createdAt)));document.getElementById('factMetrics').innerHTML=`<div class="metric"><div class="label">Получено</div><div class="value income">${fmt(a.income)}</div></div><div class="metric"><div class="label">Потрачено</div><div class="value expense">${fmt(a.expense)}</div></div><div class="metric"><div class="label">Незапланированный эффект</div><div class="value ${u<0?'negative':'positive'}">${fmt(u,t