rue)}</div></div><div class="metric"><div class="label">Прогноз свободного</div><div class="value ${f.free<0?'negative':'positive'}">${fmt(f.free)}</div></div><div class="metric"><div class="label">Операций</div><div class="value">${a.count}</div></div><div class="metric"><div class="label">Выбранный месяц</div><div class="value" style="font-size:18px">${monthShort(m)}</div></div>`;
 if(!ops.length){document.getElementById('operationsTable').innerHTML='<div class="ops-empty">Пока пусто. Добавьте расход или доход.</div>';return}
 document.getElementById('operationsTable').innerHTML=`<table class="ops-table"><thead><tr><th>Дата</th><th class="left">Операция</th><th>Учёт</th><th>Категория</th><th>Сумма</th><th></th></tr></thead><tbody>${ops.map(t=>{const c=categoryById(t.categoryId);return `<tr><td>${new Date(t.date+'T12:00:00').toLocaleDateString('ru-RU')}</td><td class="left"><b>${esc(t.name)}</b>${t.note?`<div class="muted">${esc(t.note)}</div>`:''}</td><td>${t.mode==='unplanned'?'Незаплан.':'По плану'}</td><td>${c?esc(c.name):'—'}</td><td class="${t.kind}">${t.kind==='income'?'+':'−'}${fmt(t.amount)}</td><td><button class="micro" data-edit-op="${esc(t.id)}">изменить</button></td></tr>`}).join('')}</tbody></table>`;document.querySelectorAll('[data-edit-op]').forEach(b=>b.onclick=()=>openOperation({id:b.dataset.editOp}))}

function nextIncomeInfo(){const now=new Date(),cur=currentMonthInHorizon();let best=null;state.cats.filter(c=>c.kind==='income'&&c.day!=='spread').forEach(c=>{for(let k=0;k<3;k++){const d=new Date(now.getFullYear(),now.getMonth()+k,Number(c.day));const m=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;if(d>now&&amountFor(c,m)>0&&!isSettled(c,m)){if(!best||d<best.date)best={date:d,cat:c,amount:amountFor(c,m)};break}}});if(!best)return null;return {...best,days:Math.ceil((best.date-now)/86400000),cur}}
function renderToday(){const now=new Date(),m=currentMonthInHorizon(),f=forecastData(m),next=nextIncomeInfo(),days=daysInMonth(m),curDay=Math.min(now.getDate(),days),remainingDays=Math.max(1,days-curDay+1),spreadRemaining=Math.max(0,spreadTotal(m)-actualSpreadExpense(m)),daily=spreadRemaining/remainingDays,half=(now.getDate()>=15&&now.getDate()<=29)?'вторая':'первая';document.getElementById('todayCard').innerHTML=`<div class="card-head"><div><h3>Сегодня</h3><p>${now.toLocaleDateString('ru-RU',{day:'numeric',month:'long',year:'numeric'})}</p></div><span class="pill">${half} половина</span></div><div class="today-main">${fmt(daily)} в день</div><div class="muted">остаток плановых равномерных трат ÷ оставшиеся дни</div><div class="mini-grid"><div class="mini"><span class="muted">До следующего прихода</span><b>${next?next.days+' дн.':'—'}</b>${next?`<small>${esc(next.cat.name)} · ${fmt(next.amount)}</small>`:''}</div><div class="mini"><span class="muted">Можно добавить сверх плана</span><b class="${f.free<0?'negative':'positive'}">${fmt(f.free)}</b><small>без изменения остальных планов</small></div><div class="mini"><span class="muted">Равномерных трат осталось</span><b>${fmt(spreadRemaining)}</b><small>если вы заносите их как факт по категориям</small></div></div>`}
function renderWarnings(){const c=chronic(),cum=cumulativeSavings(),m70=milestone(cum,70000),m210=milestone(cum,210000),m=state.selectedMonth,f=forecastData(m),u=unplannedImpact(m),alerts=[];alerts.push(`<div class="alert ${c.min<0?'red':'green'}"><b>Первая половина:</b> хронический минимум ${fmt(c.min)}. Рекомендуемый неснижаемый остаток — <b>${fmt(c.recommended)}</b>.</div>`);if(u<0)alerts.push(`<div class="alert ochre"><b>${monthLabel(m)}:</b> незапланированные операции уже забрали ${fmt(Math.abs(u))} из свободного остатка.</div>`);if(f.free<3000)alerts.push(`<div class="alert ${f.free<0?'red':'ochre'}"><b>Свободный остаток по прогнозу:</b> ${fmt(f.free)}. ${f.free<0?'План сейчас не сходится.':'Запас меньше 3 000 ₽.'}</div>`);else alerts.push(`<div class="alert green"><b>Текущий прогноз:</b> после плана и внесённых отклонений остаётся ${fmt(f.free)}.</div>`);alerts.push(`<div class="alert green"><b>Подушка:</b> 70 000 ₽ — ${m70?monthLabel(m70):'не достигается'}, 210 000 ₽ — ${m210?monthLabel(m210):'не достигается'}.</div>`);document.getElementById('warningsCard').innerHTML=`<div class="card-head"><div><h3>На что обратить внимание</h3><p>План + фактические отклонения</p></div></div>${alerts.join('')}`}
function renderActions(){const m=state.selectedMonth,groups={};function add(day,html){const k=String(day);(groups[k]||(groups[k]=[])).push(html)}state.cats.forEach(c=>{const a=amountFor(c,m);if(!a||c.day==='spread')return;const done=isSettled(c,m);add(c.day,`<div class="task"><b class="${c.kind}">${c.kind==='income'?'+':'−'}${fmt(a)}</b> · ${esc(c.name)} ${done?'<span class="tag">факт внесён</span>':''}${c.freq==='once'?'<br><small>разовая статья</small>':''}</div>`)});const s=Number(state.savings[m]||0);if(s!==0)add(state.savingDay,`<div class="task"><b class="saving">${s>=0?'−':'+'}${fmt(Math.abs(s))}</b> · перевод в подушку</div>`);add(30,`<div class="task"><b class="saving">${fmt(recommendedReserve(m))}</b> · безопасный резерв на начало следующего цикла<br><small>Строгий минимум: ${fmt(strictReserve(m))}; запас: 3 000 ₽.</small></div>`);if(spreadTotal(m)>0)add('весь месяц',`<div class="task"><b>${fmt(spreadTotal(m))}</b> · равномерные траты по плану<br><small>Фактом по этим категориям внесено ${fmt(actualSpreadExpense(m))}.</small></div>`);const keys=Object.keys(groups).sort((a,b)=>{if(a==='весь месяц')return 1;if(b==='весь месяц')return -1;return Number(a)-Number(b)});document.getElementById('actionsCard').innerHTML=`<div class="card-head"><div><h3>Что делать в этом месяце</h3><p>${monthLabel(m)}</p></div></div><div class="timeline">${keys.map(k=>`<div class="day-group"><div class="day-label">${/^\d+$/.test(k)?k+'-го':k}</div><div>${groups[k].join('')}</div></div>`).join('')}</div>`}
function renderScenario(){const options=[['kontur','Отменить Контур Толк','+1 400 ₽/мес'],['oneAi','Оставить одну нейросеть','+3 000 ₽/мес'],['lunch3','Обедать в вузе 3 раза в неделю','+4 333 ₽/мес'],['secondStudent','Взять второго ученика','+7 680 ₽/мес после налога'],['quitSmoking','Бросить курить','+6 933 ₽/мес']],effect=activeScenarioEffect(),from=currentMonthInHorizon(),base=cumulativeSavings(),sc=cumulativeSavings(effect,from),end=monthList().at(-1);document.getElementById('scenarioCard').innerHTML=`<div class="card-head"><div><h3>Что если</h3><p>Сценарии не меняют план — только показывают эффект</p></div></div><div class="lever-list">${options.map(([id,t,sub])=>`<label class="lever"><input type="checkbox" data-lever="${id}" ${state.levers[id]?'checked':''}><span><b>${t}</b><small>${sub}</small></span></label>`).join('')}</div><div class="scenario-result"><div><span class="muted">Без изменений</span><div><b>${fmt(base[end])}</b></div></div><div><span class="muted">Со сценариями</span><div><strong>${fmt(sc[end])}</strong></div></div></div>`;document.querySelectorAll('[data-lever]').forEach(el=>el.onchange=e=>{state.levers[e.target.dataset.lever]=e.target.checked;save();renderScenario()})}

function parseMoneyFromQuestion(q){const matches=String(q).replace(/\s/g,'').match(/\d+(?:[.,]\d+)?/g);if(!matches)return null;const nums=matches.map(x=>Number(x.replace(',','.'))).filter(Number.isFinite);return nums.length?Math.max(...nums):null}
function remainingFixed(m,kind){return state.cats.filter(c=>c.kind===kind&&c.day!=='spread'&&amountFor(c,m)>0&&!isSettled(c,m)).map(c=>({name:c.name,amount:amountFor(c,m),day:Number(c.day)||0})).sort((a,b)=>a.day-b.day)}
function localAnswer(q){con